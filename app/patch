diff --git a/boot/zephyr/CMakeLists.txt b/boot/zephyr/CMakeLists.txt
index ac2ab1c9..bc731bb1 100644
--- a/boot/zephyr/CMakeLists.txt
+++ b/boot/zephyr/CMakeLists.txt
@@ -464,6 +464,27 @@ if(NOT CONFIG_BOOT_SIGNATURE_USING_KMU AND NOT CONFIG_BOOT_SIGNATURE_KEY_FILE ST
   zephyr_library_sources(${GENERATED_PUBKEY})
 endif()
 
+# Optional second public key for in-field key rotation.
+# Pass -DMCUBOOT_BAKE_PUBKEY_2=<path> to west build to enable.
+if(DEFINED CONFIG_BOOT_SECOND_SIGNATURE_KEY_FILE AND
+   NOT CONFIG_BOOT_SECOND_SIGNATURE_KEY_FILE STREQUAL "")
+  set(GENERATED_PUBKEY_2 ${ZEPHYR_BINARY_DIR}/autogen-pubkey-2.c)
+  execute_process(
+    COMMAND ${PYTHON_EXECUTABLE} ${MCUBOOT_DIR}/scripts/imgtool.py getpub
+            -k ${CONFIG_BOOT_SECOND_SIGNATURE_KEY_FILE}
+    OUTPUT_VARIABLE _raw_pub_key_2
+    RESULT_VARIABLE _result
+  )
+  if(NOT _result EQUAL 0)
+    message(FATAL_ERROR "MCUboot: failed to extract second public key from ${CONFIG_BOOT_SECOND_SIGNATURE_KEY_FILE}")
+  endif()
+  string(REPLACE "ecdsa_pub_key" "ecdsa_pub_key_2" _raw_pub_key_2 "${_raw_pub_key_2}")
+  file(WRITE ${GENERATED_PUBKEY_2} "${_raw_pub_key_2}")
+  zephyr_library_sources(${GENERATED_PUBKEY_2})
+  zephyr_compile_definitions(MCUBOOT_SIGN_EC256_SECOND_KEY)
+  message(STATUS "MCUboot: second public key baked in for key rotation (${CONFIG_BOOT_SECOND_SIGNATURE_KEY_FILE})")
+endif()
+
 if(CONFIG_BOOT_ENCRYPTION_KEY_FILE AND NOT CONFIG_BOOT_ENCRYPTION_KEY_FILE STREQUAL "")
   # CONF_FILE points to the KConfig configuration files of the bootloader.
   unset(CONF_DIR)
diff --git a/boot/zephyr/Kconfig b/boot/zephyr/Kconfig
index b5e1bb0e..6b96eebe 100644
--- a/boot/zephyr/Kconfig
+++ b/boot/zephyr/Kconfig
@@ -507,6 +507,16 @@ config BOOT_SIGNATURE_KEY_FILE
 	  with the public key information will be written in a format expected by
 	  MCUboot.
 
+config BOOT_SECOND_SIGNATURE_KEY_FILE
+	string "Second PEM key file for in-field key rotation"
+	default ""
+	help
+	  Optional second public key (absolute path to a PEM file). When set,
+	  MCUboot is compiled with two verification keys so that images signed
+	  with either key are accepted. Used during the transition phase of a
+	  key rotation. Must be an absolute path; set via MCUBOOT_BAKE_PUBKEY_2
+	  on the west build command line.
+
 endif
 
 config SOC_NRF54LX_SKIP_GLITCHDETECTOR_DISABLE
diff --git a/boot/zephyr/keys.c b/boot/zephyr/keys.c
index ab403ddc..b31dfcea 100644
--- a/boot/zephyr/keys.c
+++ b/boot/zephyr/keys.c
@@ -37,6 +37,11 @@ extern unsigned int rsa_pub_key_len;
 #elif defined(MCUBOOT_SIGN_EC256)
 extern const unsigned char ecdsa_pub_key[];
 extern unsigned int ecdsa_pub_key_len;
+/* Optional second key for in-field key rotation (MCUBOOT_SIGN_EC256_SECOND_KEY). */
+#if defined(MCUBOOT_SIGN_EC256_SECOND_KEY)
+extern const unsigned char ecdsa_pub_key_2[];
+extern unsigned int ecdsa_pub_key_2_len;
+#endif
 #elif defined(MCUBOOT_SIGN_ED25519)
 extern const unsigned char ed25519_pub_key[];
 extern unsigned int ed25519_pub_key_len;
@@ -62,8 +67,18 @@ const struct bootutil_key bootutil_keys[] = {
         .len = &ed25519_pub_key_len,
 #endif
     },
+#if defined(MCUBOOT_SIGN_EC256_SECOND_KEY)
+    {
+        .key = ecdsa_pub_key_2,
+        .len = &ecdsa_pub_key_2_len,
+    },
+#endif
 };
-const int bootutil_key_cnt = 1;
+const int bootutil_key_cnt = 1
+#if defined(MCUBOOT_SIGN_EC256_SECOND_KEY)
+    + 1
+#endif
+    ;
 #endif /* HAVE_KEYS */
 #else
 unsigned int pub_key_len;
